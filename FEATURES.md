# Features

Wanted features, next first. Each entry says what it is, how it should behave,
and where the code lives. Delete an entry when it ships — the git history keeps
the record.

Nothing outstanding. The list is empty on purpose rather than by neglect: the
last of it — the README cut, the join URL in the add-machine dialog, a
report-bug button, a tool for listing templates, a quicker reach to the zoom
snap, terminals that stay legible while you zoom out, and environment variables
on a template — has shipped.
